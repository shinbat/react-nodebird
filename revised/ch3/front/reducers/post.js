export const initilalState = {
    mainPosts: [],
};

const reducer = (state = initilalState, action) => {
    switch (action.type) {
        default:
            return state;
    };
};

export default reducer;